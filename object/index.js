// JavaScript Object

const user = {
    name: 'Pankaj Sharma',
    age : 25,
    hobby: 'Playing Chess',
}
console.log(user);

// Access a Perticuler item from the object via two types 1 is dot notation , break notation 

// Access the name from the user object using dot notation 
console.log(user.name);

// Access the age from the user object using the break notation 
console.log(user['age']);
